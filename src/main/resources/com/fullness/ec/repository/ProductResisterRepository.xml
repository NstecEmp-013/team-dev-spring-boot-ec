<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fullness.ec.repository.ProductResisterRepository">

    <!-- すべての商品を取得 -->
    <select id="selectAll" resultType="com.fullness.ec.entity.Product">
        SELECT
            id,
            name,
            price,
            image_url As imageUrl,
            product_category_id AS productCategoryId
        FROM product
        WHERE delete_flag = 0
    </select>

     <!-- 商品を登録 -->
    <insert id="insert" parameterType="com.fullness.ec.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product
            (name, price, product_category_id, image_url, delete_flag)
        VALUES
            (#{name}, #{price}, #{productCategoryId}, #{imageUrl}, 0)
    </insert>

</mapper>