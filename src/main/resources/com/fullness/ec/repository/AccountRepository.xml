<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fullness.ec.repository.AccountRepository">
    <select id="selectAll" resultType="com.fullness.ec.entity.Employee">
      SELECT e.name AS empName, e.name_kana AS empKana, e.department_id AS empDepartmentId
	  FROM employee e
	  LEFT JOIN employee_account ea ON e.name = ea.name
      WHERE ea.employee_id IS NULL;
    </select>
    <select id="selectByAccountName" resultType="com.fullness.ec.entity.AccountCategories">
        SELECT a.name AS accountName, a.password AS password, a.employee_id AS accountId
        FROM employee_account a
        WHERE a.name = #{accountName}
    </select>
</mapper>